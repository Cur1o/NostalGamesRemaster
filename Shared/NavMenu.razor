<MudNavMenu Bordered="true" Color="Color.Primary" Rouded="true" Class="pa-2">
    <MudNavLink Class="my-2" Icon="@Icons.Material.Filled.Home" IconColor="Color.Default" Href="/" Match="NavLinkMatch.All">Home</MudNavLink>
    <MudNavLink Class="my-2" Icon="@Icons.Material.Filled.Info" IconColor="Color.Default" Href="/PacMan">Pac-Man</MudNavLink>
</MudNavMenu>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}